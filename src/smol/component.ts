class Component {
    parent: any
    id: any
    name: any

    constructor(props: any) {
        this.parent = props.parent
        this.id = props.id
        this.name = props.name
    }
}

export default Component
