features:
post render method that modifies render dom before it is send using jsdom to make do
multiple pages (instance tree per page)
proper passing stuff as a child
gziping ws sending data as blob
ASYNC Handlebars
Handlebars.create new instance for each page?

replace handlebars with custom solution maybe even jsx?

bugs:


feature bug fixes:
fork dom-diff or write more efficient one that will produce smaller diffs
 reasoing: something wierd with dom diff, works but unnecesery updates
